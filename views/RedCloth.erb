<%require 'redcloth'

text = %{Hello,
	This is some multiline text, which hopefully
	will be converted to html.  Let's see if
	this will work.  The next line will be indented
	       to see if this kind of formatting is
	maintained.
	
	Well, that didn't work.  This should be a new 
	paragraph.  Does it work?}

just_text = RedCloth.new text
%>
<%=just_text.to_html%>